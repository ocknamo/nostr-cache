- タスク完了時に skeptical-reviewer を呼び出す
- PR作成後に pr-finalizer を呼び出す
- コメントには**コードを読めば分かることを書かない**。書くのは「なぜそうしたか」
  （選ばなかった案とその理由、踏んだ失敗、仕様上の制約）だけにする
  - 型・引数・戻り値・処理の流れを言い換えただけの doc コメントは削る
  - 同じ説明を 2 箇所に書かない。既存の要素・関数と同じ挙動なら、繰り返さずに
    そちらを参照する
  - 根拠は 1 回で言い切る。同じ理由を段落を変えて言い直さない
