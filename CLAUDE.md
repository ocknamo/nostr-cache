- タスク完了時に skeptical-reviewer を呼び出す
- PR作成後に pr-finalizer を呼び出す
- コメントにはコードを読めば分かることを書かない
- コメントは冗長にしない。非自明な理由だけを短く書き、経緯・計測値・言い換えは残さない
- 名前を言い換えただけの doc コメントは書かない。書くことがなければ付けない
- 1 つの doc コメントは原則 3 行以内。段落を分けるのは、分けないと誤読される場合だけ
- 同じ理由を doc コメントと行内コメントの両方に書かない。どちらか一方
- コードを移動・分割するとき、元のコメントをそのまま運ばない。移動先で必要か毎回判断する
